[project]
name = "tongue-frontend"
version = "0.1.0"
description = "Streamlit frontend for tongue diagnosis"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.31",
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tongue_frontend"]
